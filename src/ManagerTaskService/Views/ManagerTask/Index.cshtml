@model ManagerTask.Data.Driver

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@if (ViewBag.Error != null)
{
    <p class="text-danger">@ViewBag.Error</p>
}
else
{
    if (Model != null)
    {
        <div>
            <label>Name</label> @Model.Name
        </div>
        <div>
            <label>Email</label> @Model.Manager.Name
        </div>
    }
    else
    {
        <p class="text-danger">No driver found</p>
    }
}
